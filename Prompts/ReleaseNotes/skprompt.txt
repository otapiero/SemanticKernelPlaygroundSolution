Generate clear, concise, and professional release notes for version {{$version}} using the Git commit messages provided below.
Make the realese explenatory and easy to read.

Categorize the notes into logical sections such as Features, Bug Fixes, Improvements, Refactoring, or Other Changes, based on the content of each commit.
Use bullet points within each category for readability.
Format the entire output in Markdown.
Add a header with the version number and date (if provided, use {{$date}}; otherwise, leave the date blank).

Append the following disclaimer at the end of the release notes:
Release Notes is automatically generated by AI.

Input Commits:
{{$commitInfo}}